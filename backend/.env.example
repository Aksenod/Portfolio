# Required
JWT_SECRET=change-me-super-secret

# Admin credentials
ADMIN_USERNAME=admin
# Option A (recommended): store a bcrypt hash
ADMIN_PASSWORD_HASH=
# Option B (dev only): store plain password (will be hashed on startup)
ADMIN_PASSWORD=bugrov2025

# DB
# Render will provide DATABASE_URL automatically for Postgres.
# Locally you can use SQLite:
DATABASE_URL=sqlite:///./app.db

# CORS (comma-separated)
FRONTEND_ORIGINS=http://localhost:4173,http://127.0.0.1:4173,https://<your-github-pages-domain>

# JWT expiry in minutes (default: 43200 = 30 days)
JWT_EXPIRES_MINUTES=43200
